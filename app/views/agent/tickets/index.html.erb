<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1><%= t('.title') %></h1>
      <div class="table-responsive">
        <table class="table table-striped">
          <thead>
          <tr>
            <th>Title</th>
            <th>Status</th>
            <th>Created at</th>
            <th>Updated at</th>
          </tr>
          </thead>
          <tbody>
          <% collection.each do |ticket| %>
              <tr>
                <th><%= link_to ticket.title, agent_ticket_path(ticket) %></th>
                <th><%= ticket.status %></th>
                <th><%= l ticket.created_at, format: :short %></th>
                <th><%= l ticket.updated_at, format: :short %></th>
              </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
